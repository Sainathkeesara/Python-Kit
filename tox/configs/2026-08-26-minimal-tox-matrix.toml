# last_verified: 2026-08-26 - tox n/a

[tool.tox]
legacy_tox_ini = """
[tox]
envlist = py39,py310,py311,py312
[testenv]
deps = pytest, coverage
commands = coverage run -m pytest, coverage report
[testenv:lint]
skip_install = true
deps = ruff
commands = ruff check .
"""
