# last_verified: 2026-07-19 · uv 0.5.x
[project]
name = "demo-cli"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["requests>=2.31"]

[project.scripts]
greet = "demo_cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
